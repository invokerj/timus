#include <stdio.h>
#include <string.h>

#define N 250001

char s[N];
char t[N];

int main (void)
{
	int i,j,k,n;
	scanf ("%d", &n);
	scanf ("%s", s);
	scanf ("%s", t);
	for (i=0;i<n;i++) {
		j=i;
		while (t[]!=s[i]) j++;
		for (k=j;k%n != j;k++) {

		}
	}


}
